# Non-secret defaults. External file and environment override these values.
jdbcUrl=jdbc:mysql://localhost:3306/qatrack
username=qatrack_app
initialPoolSize=2
maxPoolSize=8
acquireTimeout=3000
# Password deliberately absent. acquireTimeout is milliseconds.
