# Copy to config/database-test.local.properties; use a disposable EMPTY schema.
# Never use qatrack. The test account needs DDL rights on this test schema only.
jdbcUrl=jdbc:mysql://127.0.0.1:3306/qatrack_test_r1
username=qatrack_test
password=
