# Copy to config/database.local.properties (Git ignored, outside the WAR).
# Or select an external file with QATRACK_DB_CONFIG / -Dqatrack.db.config.
jdbcUrl=jdbc:mysql://localhost:3306/qatrack
username=qatrack_app
password=
initialPoolSize=2
maxPoolSize=8
acquireTimeout=3000
